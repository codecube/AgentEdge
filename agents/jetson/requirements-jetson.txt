# PyTorch for Jetson Orin Nano — JetPack 6.x / CUDA 12.6
# Install this BEFORE the main requirements.txt:
#   pip install -r agents/jetson/requirements-jetson.txt
#   pip install -r agents/jetson/requirements.txt
#
# The standard pip torch wheel is CPU-only on aarch64.
# NVIDIA's Jetson AI Lab index provides wheels with CUDA support.
# Requires Python 3.10 (cp310).

--index-url https://pypi.jetson-ai-lab.io/jp6/cu126
--extra-index-url https://pypi.org/simple

numpy==1.26.1
torch==2.9.1
