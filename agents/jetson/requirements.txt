fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
pydantic>=2.5.0
websockets>=12.0
mcp>=1.0.0
pyserial>=3.5
transformers>=4.36.0

# PyTorch — install BEFORE this file on Jetson (see requirements-jetson.txt)
# On non-Jetson machines (x86_64 / Mac) the standard pip wheel works:
torch>=2.1.0
